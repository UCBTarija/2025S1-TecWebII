function adcionarOp(operador) {
    let display = document.getElementById('operador');
    display.value = operador;
}

function adcionarNum(numero) {
    let display = document.getElementById('display');
    display.value += numero;
}